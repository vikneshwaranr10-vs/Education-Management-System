spring:
  datasource:
    url: jdbc:mysql://localhost:3306/education_db
    username: root
    password: Root@123

server:
  port: 8081

logging:
  level:
    com.example: DEBUG

file:
  upload-dir: uploads